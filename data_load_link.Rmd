---
title: "load instacart data and plot it!"
author: "wq2151"
date: "10/27/2019"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(p8105.datasets)
library(tidyverse)
library(plotly)
```

```{r is this the problem, warning=FALSE}
data("instacart")

most_aisle_orders = instacart %>%
  count(aisle, sort = T)
  
most_aisle_orders %>% 
  filter(n > 10000) %>% 
  mutate(aisle = fct_reorder(aisle, -n)) %>% 
  plot_ly(x = ~aisle, y = ~n, type = "bar", color = ~aisle)
```




